/** URL fragment and matching DOM id for the Email Jobs block on Manage → Email (router anchor scrolling). */
export const EMAIL_JOBS_SECTION_ANCHOR = 'email-jobs';

/** Query param carrying the job row to scroll into view after returning from job detail. */
export const EMAIL_JOBS_FOCUS_JOB_QUERY_PARAM = 'focusJob';
